{
  "routes": [
    {
      "method": "GET",
      "path": "/submission",
      "handler": "Submission.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/submission/count",
      "handler": "Submission.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/submission/:_id",
      "handler": "Submission.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/submission",
      "handler": "Submission.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/submission/:_id",
      "handler": "Submission.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/submission/:_id",
      "handler": "Submission.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
